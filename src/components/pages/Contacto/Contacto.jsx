import React from 'react';

const Contacto = () => {
    return (
        <section className='container'>
            <h2>Pagina de contacto</h2>
        </section>
    );
};

export default Contacto;